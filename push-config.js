/* Настройки push-уведомлений.
   Заполняется ПОСЛЕ развёртывания сервера (см. push/README.md).
   Оба значения публичные — их безопасно хранить в репозитории.
   Можно не трогать этот файл: те же поля есть в Настройках приложения. */
window.PUSH_CONFIG = {
  serverUrl: "",       // напр. "https://kopilka-push.<твой-аккаунт>.workers.dev"
  vapidPublicKey: ""   // публичный ключ VAPID из genkeys.mjs
};
